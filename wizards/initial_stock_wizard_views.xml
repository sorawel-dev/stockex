<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Vue Formulaire pour Assistant Stock Initial -->
    <record id="view_initial_stock_wizard_form" model="ir.ui.view">
        <field name="name">stockex.initial.stock.wizard.form</field>
        <field name="model">stockex.initial.stock.wizard</field>
        <field name="arch" type="xml">
            <form string="Assistant Stock Initial">
                <field name="state" invisible="1"/>
                <field name="progress" invisible="1"/>
                
                <!-- Barre de progression -->
                <div class="alert alert-info" role="status" invisible="progress == 0.0">
                    <div style="margin-bottom: 10px;">
                        <strong>🔄 <field name="progress_message" readonly="1" nolabel="1"/></strong>
                    </div>
                    <div style="background: #e9ecef; border-radius: 5px; height: 30px; overflow: hidden; position: relative;">
                        <div style="background: linear-gradient(90deg, #28a745 0%, #20c997 100%); height: 100%; display: flex; align-items: center; justify-content: center; transition: width 0.3s ease;">
                            <span style="color: white; font-weight: bold; font-size: 14px; position: absolute; left: 50%; transform: translateX(-50%); z-index: 1;">
                                <field name="progress" widget="percentage" nolabel="1"/>
                            </span>
                        </div>
                    </div>
                </div>
                
                <!-- Étape 1 : Import -->
                <group invisible="state != 'step1'">
                    <group>
                        <field name="name"/>
                        <field name="date"/>
                        <field name="location_id" options="{'no_create': True}"/>
                        <label for="location_id" string="" invisible="1"/>
                        <div class="text-muted" style="font-size: 11px; margin-top: -8px;">
                            <i>⚠️ Optionnel : Utilisé uniquement si la colonne ENTREPOT est absente du fichier Excel</i>
                        </div>
                    </group>
                    <group>
                        <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}"/>
                        <field name="create_products"/>
                        <field name="create_categories"/>
                        <field name="create_warehouses"/>
                    </group>
                </group>
                
                <group string="⚠️ Options Avancées (DANGER)" invisible="state != 'step1'">
                    <field name="force_reset" 
                           widget="boolean_toggle"
                           class="text-danger"/>
                    <div class="alert alert-danger" role="alert" invisible="not force_reset" style="margin: 10px 0;">
                        <strong>🔥 ATTENTION - ACTION IRRÉVERSIBLE !</strong><br/>
                        Cette option va :<br/>
                        • ❌ Supprimer TOUS les mouvements de stock<br/>
                        • ❌ Remettre TOUTES les quantités à 0<br/>
                        • ❌ Effacer tout l'historique de stock<br/><br/>
                        <strong style="color: #dc3545;">⚠️ NE PAS UTILISER EN PRODUCTION</strong> sauf réinitialisation complète volontaire.
                    </div>
                    
                    <field name="incremental_update" 
                           widget="boolean_toggle"
                           class="text-success"/>
                    <div class="alert alert-success" role="alert" invisible="not incremental_update" style="margin: 10px 0;">
                        <strong>🔄 MODE INCRÉMENTAL ACTIVÉ</strong><br/>
                        L'import continuera même si des stocks existent déjà :<br/>
                        • ✅ Les produits existants seront mis à jour (ajout de quantité)<br/>
                        • ✅ Les nouveaux produits seront créés<br/>
                        • 📊 Utile pour compléter un import partiel<br/><br/>
                        <strong style="color: #28a745;">ℹ️ Les stocks existants ne seront PAS supprimés</strong>
                    </div>
                </group>
                
                <group string="Import Excel (Requis)" invisible="state != 'step1'">
                    <p class="text-muted">
                        <strong>Format du fichier Excel :</strong><br/>
                        Colonnes requises : CODE PRODUIT, NOM PRODUIT, QUANTITE, PRIX UNITAIRE<br/>
                        Colonnes optionnelles : CATEGORIE, CODE CATEGORIE, <strong style="color: #00A09D;">ENTREPOT</strong><br/>
                        <strong style="color: #00A09D;">✅ Multi-entrepôts supporté :</strong> Une ligne par produit/entrepôt<br/>
                        <i style="color: #e74c3c;">Le fichier Excel est OBLIGATOIRE pour initialiser le stock.</i>
                    </p>
                    <field name="filename" invisible="1"/>
                    <field name="import_file" filename="filename"/>
                </group>
                
                <div class="alert alert-warning" role="alert" invisible="state != 'step1'">
                    <strong>⚠️ ATTENTION :</strong> Cet assistant est destiné aux <strong>bases de données VIDES</strong> (sans stock existant).<br/>
                    Il créera directement les <strong>stocks initiaux dans les quants</strong> (pas d'inventaire).<br/><br/>
                    <strong>🔄 Si des stocks existent déjà :</strong> Cochez l'option "Mise à jour incrémentale" pour continuer l'import.<br/>
                    Pour une gestion complète du stock, utilisez un inventaire normal.
                </div>
                
                <div class="alert alert-info" role="alert" invisible="state != 'step1'">
                    <strong>📧 💬 📱 Notifications :</strong> Les notifications par Email, WhatsApp et Telegram sont configurées dans 
                    <strong>Configuration → Paramètres → Gestion d'Inventaire</strong>.
                </div>
                
                <!-- Étape 2 : Prévisualisation -->
                <group invisible="state != 'step2'">
                    <field name="preview_summary" nolabel="1" widget="html"/>
                </group>
                
                <footer>
                    <!-- Boutons étape 1 -->
                    <button name="action_preview" type="object" string="🔍 Prévisualiser" class="btn-primary" 
                            invisible="state != 'step1' or not import_file"/>
                    <button name="action_create_initial_stock" type="object" string="✅ Créer Stock Initial" class="btn-success" 
                            invisible="state != 'step1' or not import_file" confirm="Confirmez-vous la création du stock initial ?"/>
                    
                    <!-- Boutons étape 2 -->
                    <button name="action_back" type="object" string="← Retour" class="btn-secondary" 
                            invisible="state != 'step2'"/>
                    <button name="action_create_initial_stock" type="object" string="✅ Confirmer l'import" class="btn-primary" 
                            invisible="state != 'step2'" confirm="Confirmez-vous l'import du stock initial ?"/>
                    
                    <button string="Annuler" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
    
    <!-- Action pour l'Assistant Stock Initial -->
    <record id="action_initial_stock_wizard" model="ir.actions.act_window">
        <field name="name">Stock Initial</field>
        <field name="res_model">stockex.initial.stock.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
        
</odoo>
