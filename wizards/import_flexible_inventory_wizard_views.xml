<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Vue Formulaire Import Flexible -->
    <record id="view_import_flexible_inventory_wizard_form" model="ir.ui.view">
        <field name="name">stockex.import.flexible.inventory.wizard.form</field>
        <field name="model">stockex.import.flexible.inventory.wizard</field>
        <field name="arch" type="xml">
            <form string="Import d'Inventaire">
                <field name="state" invisible="1"/>
                
                <!-- Étape 1: Configuration -->
                <group invisible="state != 'draft'">
                    <group>
                        <field name="name"/>
                        <field name="date"/>
                        <field name="filename" invisible="1"/>
                        <field name="file" filename="filename"/>
                    </group>
                    <group>
                        <field name="create_products"/>
                        <field name="create_categories"/>
                        <field name="create_warehouses"/>
                        <field name="create_locations"/>
                        <field name="update_prices"/>
                        <field name="default_price"/>
                    </group>
                </group>
                
                <group string="Multi-Entrepôts" invisible="state != 'draft'">
                    <field name="multi_warehouse_mode" widget="radio"/>
                    <div class="text-muted" style="margin-top: 10px;">
                        <p><strong>Inventaire Global</strong> : Toutes les données dans un seul inventaire (recommandé pour analyse globale)</p>
                        <p><strong>Inventaires Séparés</strong> : Un inventaire par entrepôt (recommandé pour validation séparée par magasin)</p>
                    </div>
                </group>
                
                <div class="alert alert-info" role="alert" invisible="state != 'draft'">
                    <strong>📋 Format Flexible</strong><br/>
                    Ce wizard détecte automatiquement vos colonnes :<br/>
                    <ul style="margin-top: 10px;">
                        <li><strong>CODE PRODUIT</strong> : Material, Code d'article, Code...</li>
                        <li><strong>QUANTITE</strong> : Quantité, quantite, Quantity...</li>
                        <li><strong>PRODUIT</strong> (optionnel) : Material Description, Nom du Materiel...</li>
                        <li><strong>PRIX UNITAIRE</strong> (optionnel) : Prix, Price... (sinon utilise prix par défaut)</li>
                        <li><strong>CATEGORIE</strong> (optionnel) : Type d'article, Category...</li>
                        <li><strong>ENTREPOT</strong> (optionnel) : Magasin, Warehouse, Location...</li>
                        <li><strong>SOUS-MAGASIN</strong> (optionnel) : Nom du Sous-magasin, Sub-location...</li>
                        <li><strong>RANGER/RAYON</strong> (optionnel) : Ranger, Rayon, Aisle...</li>
                        <li><strong>CASIER</strong> (optionnel) : Casier, Étagère, Rack, Shelf...</li>
                    </ul>
                    <p style="margin-top: 10px;"><em>Les emplacements enfants sont automatiquement créés sous l'entrepôt parent pour organiser votre stock (ex: Entrepôt → Sous-magasin → Ranger → Casier)</em></p>
                </div>
                
                <!-- Étape 2: Prévisualisation -->
                <group invisible="state != 'preview'">
                    <field name="preview_summary" nolabel="1" widget="html"/>
                </group>
                
                <footer>
                    <!-- Boutons étape 1 -->
                    <button name="action_preview" type="object" string="🔍 Prévisualiser" class="btn-primary" 
                            invisible="state != 'draft'"/>
                    
                    <!-- Boutons étape 2 -->
                    <button name="action_import" type="object" string="✅ Confirmer l'Import" class="btn-primary" 
                            invisible="state != 'preview'"/>
                    
                    <button string="Annuler" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
    
    <!-- Action Wizard -->
    <record id="action_import_flexible_inventory_wizard" model="ir.actions.act_window">
        <field name="name">Import Inventaire (Excel/CSV)</field>
        <field name="res_model">stockex.import.flexible.inventory.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
    
</odoo>
