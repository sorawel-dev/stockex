<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Vue formulaire héritée pour ajouter la géolocalisation -->
    <record id="view_location_form_geoloc" model="ir.ui.view">
        <field name="name">stock.location.form.geoloc</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form"/>
        <field name="arch" type="xml">
            
            <!-- Smart buttons: dans le button_box existant -->
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_open_stock_value" type="object" class="oe_stat_button">
                    <field name="stock_value" widget="statinfo" string="Valeur du stock"/>
                </button>
                <button name="action_open_moves" type="object" class="oe_stat_button">
                    <field name="move_count" widget="statinfo" string="Mouvements"/>
                </button>
                <button name="action_open_quants" type="object" class="oe_stat_button">
                    <field name="quant_count" widget="statinfo" string="Articles en stock"/>
                </button>
            </xpath>
            <!-- Ajouter les champs GPS après le champ company_id -->
            <xpath expr="//field[@name='company_id']" position="after">
                <separator string="Géolocalisation" colspan="2"/>
                <field name="latitude" widget="float"/>
                <field name="longitude" widget="float"/>
                <field name="coordinates" readonly="1"/>
                <field name="google_maps_url" invisible="1"/>
            </xpath>
            
            <!-- Ajouter les informations de contact après location_id -->
            <xpath expr="//field[@name='location_id']" position="after">
                <field name="city" placeholder="Ville"/>
                <field name="address" placeholder="Adresse complète..." colspan="2"/>
                <field name="phone" widget="phone" placeholder="+237 XX XX XX XX XX"/>
                <field name="email" widget="email" placeholder="contact@sorawel.com"/>
            </xpath>
            
            <!-- Affichage explicite: code entrepôt + nom complet -->
            <xpath expr="//field[@name='name']" position="after">
                <field name="display_name" readonly="1" string="Affichage" class="fw-bold" style="white-space: nowrap;"/>
            </xpath>
            
        </field>
    </record>

    <!-- Vue liste héritée pour afficher les coordonnées -->
    <record id="view_location_list_geoloc" model="ir.ui.view">
        <field name="name">stock.location.list.geoloc</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_tree2"/>
        <field name="arch" type="xml">
            <xpath expr="//list" position="inside">
                <field name="city" optional="hide"/>
                <field name="coordinates" optional="hide"/>
                <field name="warehouse_id" optional="show"/>
                <field name="display_name" string="Affichage" optional="show"/>
            </xpath>
        </field>
    </record>

    <!-- Vue recherche héritée -->
    <record id="view_location_search_geoloc" model="ir.ui.view">
        <field name="name">stock.location.search.geoloc</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_search"/>
        <field name="arch" type="xml">
            <field name="complete_name" position="after">
                <field name="display_name" string="Affichage (CODE | Nom)"/>
                <field name="city"/>
                <field name="country_id"/>
                <field name="warehouse_id"/>
            </field>
            <filter name="usage" position="after">
                <separator/>
                <filter string="🏠 Interne" name="usage_internal" domain="[('usage','=','internal')]"/>
                <filter string="👁️ Vue" name="usage_view" domain="[('usage','=','view')]"/>
                <separator/>
                <filter string="🏙️ Ville" name="group_by_city" 
                        domain="[]" context="{'group_by': 'city'}"/>
                <filter string="🌍 Pays" name="group_by_country" 
                        domain="[]" context="{'group_by': 'country_id'}"/>
                <filter string="🏢 Entrepôt" name="group_by_warehouse" domain="[]" context="{'group_by': 'warehouse_id'}"/>
                <separator/>
                <filter string="⚡ Réseau Sud (RIS)" name="network_south" domain="[('warehouse_id.eneo_region_id.network','=','south')]"/>
                <filter string="⚡ Réseau Nord (RIN)" name="network_north" domain="[('warehouse_id.eneo_region_id.network','=','north')]"/>
            </filter>
        </field>
    </record>

</odoo>
